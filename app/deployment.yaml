apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
  labels:
    app: app-spring-app-ive
    app.kubernetes.io/component: web
    app.kubernetes.io/instance: app-spring-app-ive
    app.kubernetes.io/name: app-spring-app-ive
    app.kubernetes.io/part-of: app-spring-app-ive
    app.openshift.io/runtime: java
  name: app-spring-app-ive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-spring-app-ive
  template:
    metadata:
      labels:
        app: app-spring-app-ive
    spec:
      containers:
      - name: app-spring-app-ive
        imagePullPolicy: Always
        image: quay.io/siamaksade/spring-petclinic:latest
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 45
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        ports:
        - containerPort: 8080
          protocol: TCP
        - containerPort: 8443
          protocol: TCP
        - containerPort: 8778
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 45
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 5
